---
# Example inventory file for staging environment
# Customize this file for your infrastructure

all:
  children:
    webservers:
      hosts:
        web-staging.example.com:
          ansible_host: 192.168.2.10
          ansible_user: ansible

    databases:
      hosts:
        db-staging.example.com:
          ansible_host: 192.168.2.20
          ansible_user: ansible
